---
name: NephroSite Audit – opakovaný komplexný audit
description: Pokyny pre cyklický technický, bezpečnostný, SEO a jazykový audit lekárskej referenčnej webovej aplikácie nephrosite.polascin.net (PHP, HTML5, OWASP, WCAG 2.1, GDPR, slovensko-medicínska terminológia).
---

# NephroSite Audit – opakovaný komplexný audit

## Cieľ

Vykonávať opakované hĺbkové audity lekárskeho referenčného webu **nephrosite.polascin.net** podľa tohto dokumentu a implementovať bezpečné, relevantné opravy a vylepšenia. Audit sa zameriava na:

- bezpečnosť konfigurácie, HTTP hlavičiek a kódu (OWASP, `.htaccess`, noindex pravidlá),
- syntaktickú a logickú správnosť PHP/HTML kódu,
- SEO optimalizáciu, kanonické URL, `sitemap.xml` a `robots.txt`,
- prístupnosť podľa WCAG 2.1 AA (kontrast, klávesnicová navigácia, skip-link, ARIA),
- responzívny dizajn a mobilný výkon (320 px až desktop, 44×44 px touch targets),
- integritu interných a externých odkazov a referencií,
- odbornú jazykovú korektúru slovenčiny a medicínskej terminológie (nefrológia, dialýza, transplantácia, purifikácia),
- súlad s GDPR, cookie lištou a transparentným spracovaním údajov,
- kontrolu automatického deployu na produkciu WebSupport (rsync/SSH cez GitHub Actions).

## Prehľad projektu

**nephrosite.polascin.net** je lekárska referenčná webová stránka Dr. Ľubomíra Polaščína venovaná nefrológii v slovenskom jazyku. Obsahuje:

- **Hlavnú stránku:** `index.php` (PHP s modulárnymi blokmi z `blocks/` a segmentmi z `segments/`)
- **Právnu stránku:** `privacy.php` (presmerovanie / zásady ochrany súkromia a medicínsky disclaimer)
- **Obsahové bloky (`blocks/`):** `metatags.sk.php`, `links.sk.php`, `styles.css.php`, `scripts.sk.php`, `ns.sk.php`, `footer.sk.php`, `cookie-banner.sk.php`, `archive-notice.sk.php`, `current.sk.php`, `intro.sk.php`, `info.sk.php`, `lectures.sk.php`, `promo.sk.php`, `nephrology.sk.php`, `foazosszu.sk.php`, `regcitra.sk.php`
- **Medicínske referenčné materiály (`files/`):** `nefrologia.html`, `dialyza.html`, `purifikacia.html`, `transplantacia.html`, `usgo.html`, `nefamb.html`, `lipton-tx.html`, `privacy.html`
- **Obsahové segmenty (`segments/`):** HTML fragmenty
- **Dokumentácia a Prezentácie (`docs/`):** PDF a GIF prezentácie (**Git LFS** súbory ako `nephrogame.gif`)
- **Serverová konfigurácia:** `.htaccess`, `robots.txt`, `sitemap.xml`, `.deployignore`
- **CI/CD:** `.github/workflows/deploy.yml` (GitHub Actions rsync/SSH deploy)
- **Lokálne nástroje:** `tools/install-dev-tools.ps1`, `tools/quality.ps1`, `phpstan.neon.dist`, `.php-cs-fixer.dist.php`

**KRITICKÉ OBMEDZENIE:** Agent **NESMIE nikdy modifikovať** súbory sledované cez Git LFS (najmä `docs/nephrogame.gif` a iné binárne súbory v `docs/`).

## Opakovaný postup auditu

1. Spusti audit podľa kontrolného zoznamu nižšie.
2. Zaznamenaj nálezy vrátane závažnosti, príčiny a odporúčanej opravy.
3. Implementuj bezpečné a relevantné opravy a vylepšenia.
4. Over zmeny kontrolnými nástrojmi:
   - `php -l <file.php>` pre všetky modifikované PHP súbory
   - `.\tools\quality.ps1` (PHPStan analýza + PHP-CS-Fixer kontrola)
   - Statická kontrola HTML/CSS/JS
   - HTTP hlavičky a smoke check (`curl -sIL https://nephrosite.polascin.net/`)
5. Zapíš výsledky a vyriešené nálezy do tohto `.audit.md` ako nový „Beh #X“.
6. Opakuj krok 1 až 5, kým nebudú vyriešené všetky opraviteľné nedostatky.

---

## Kontrolný zoznam auditu

### 1. Bezpečnosť (OWASP & Server)

- **HTTP bezpečnostné hlavičky v `.htaccess`:**
  - `X-Frame-Options: SAMEORIGIN`
  - `X-Content-Type-Options: nosniff`
  - `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
  - `Referrer-Policy: strict-origin-when-cross-origin`
  - `Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), usb=()`
  - `Cross-Origin-Opener-Policy: same-origin`
  - `X-Permitted-Cross-Domain-Policies: none`
  - `Options -Indexes`, `ServerSignature Off`, `Header unset X-Powered-By`
  - Blokovanie prístupu k `.git/`, `.env`, `*.key`, `*.pem`, `*.sql`, `*.log`, `*.bak`, `*.md`
- **Noindex pre súkromné cesty:**
  - Pravidlá v `.htaccess` a `robots.txt` pre `/blocks/`, `/segments/`, `/ipvrm.files/`
- **PHP bezpečnosť:**
  - `phpinfo.php` v `blocks/` — overiť, či je prístup zablokovaný alebo súbor odstránený/bezpečný
  - Správne escapovanie výstupov cez `htmlspecialchars(..., ENT_QUOTES, "UTF-8")`
  - Bezpečné `include`/`require` cesty (ochrana pred path traversal)
- **HTTPS presmerovanie:**
  - Kanonický host `https://nephrosite.polascin.net` s 301 presmerovaním.

### 2. HTML & Sémantika

- Validita HTML5 štruktúry (`<!DOCTYPE html>`, `<header>`, `<main>`, `<footer>`, `<section>`)
- Správny atribút `lang="sk"` na `<html>`
- `alt` atribúty na všetkých `<img>` elementoch
- `aria-label`, `aria-hidden`, `role` na interaktívnych a dekoratívnych prvkoch
- Skip link (`<a class="skip-link" href="#content">`) — prítomný a funkčný
- `rel="noopener noreferrer"` na všetkých `target="_blank"` odkazoch

### 3. SEOOptimalizácia

- Meta tagy: `description`, `keywords`, `author`, `robots`, `theme-color`
- Kanonické URL `<link rel="canonical" href="...">` na hlavných stránkach
- `sitemap.xml`: aktuálnosť `lastmod`, správne URL (vrátane `privacy.php`), prioritizácia
- `robots.txt`: Disallow pre interné zložky, `Crawl-delay`, odkaz na sitemapu
- Title tagy: formát `Téma | NephroSite`

### 4. Prístupnosť (WCAG 2.1 AA)

- Kontrast textu a pozadia min. 4.5:1 (3:1 pre veľký text)
- Viditeľný focus indikátor (`:focus-visible`) pri klávesnicovej navigácii
- Tab order a prístupnosť cookie bannera
- Rešpektovanie `prefers-reduced-motion`

### 5. Responzívny dizajn

- Prítomnosť `<meta name="viewport" content="width=device-width, initial-scale=1">`
- Žiadne horizontálne pretekanie obsahu (`overflow-x`) na zariadeniach 320 px–480 px
- Minimálna veľkosť touch targets 44×44 px pre interaktívne prvky
- Responzívne správanie obrázkov a tabuliek

### 6. Jazyková korektúra (Slovenčina)

- Odborná medicínska terminológia (nefrológia, dialýza, hemodialýza, peritoneálna dialýza, hemodiafiltrácia, transplantácia obličky, purifikácia krvi, glomerulonefritída, chronická obličková choroba)
- Gramatická správnosť, diakritika (dĺžne, mäkčene, háčky)
- Odborný a konzistentný štýl textov

### 7. Cookies a GDPR

- Nevyhonutné cookies povolené, nepovinné (analytické/marketingové) až po výslovnom súhlase
- Transparentná Ochrana súkromia (`privacy.php` / `files/privacy.html`) s označením prevádzkovateľa, práv GDPR, hostingu WebSupport EÚ a serverových logov
- Zdravotnícky disclaimer a archívny oznam (obsah 2019–2023, odkázanie na `nefro.polascin.net`)

### 8. Automatický Deploy (CI/CD)

- GitHub Actions workflow `.github/workflows/deploy.yml` spúšťa `php -l` lint a rsync/SSH nasadenie na WebSupport
- Dokonale zladené `.deployignore` súbory

---

## Bezpečnostné pravidlá

- Nerob nebezpečné neoverené automatické zmeny.
- Nikdy nemodifikuj súbory v Git LFS (`docs/nephrogame.gif`).
- Všetky PHP súbory musia prejsť `php -l` bez syntaktických chýb.

---

## Beh #1 – 2026-08-26

### Vykonané kontroly

- PHP syntax check `php -l` pre všetky `.php` súbory — 0 chýb.
- Kontrola `privacy.php`, `blocks/footer.sk.php`, `blocks/cookie-banner.sk.php`, `sitemap.xml`, `ipvrm.htm`.
- Konfigurácia GitHub Actions workflow `.github/workflows/deploy.yml` na nasadenie cez rsync/SSH s podporou `DEPLOY_SSH_KEY`.
- Overenie funkčnosti produkčných URL cez `curl -sIL`.

### Nálezy a opravy

1. **Právne dokumenty & Privacy Policy:**
   - Vytvorená stránka `privacy.php` so slovenským textom, prevádzkovateľom MUDr. Ľubomír Polaščín (`lubomir@polascin.net`), minimálnym rozsahom údajov (serverové logy), GDPR právami, hostingom WebSupport v EÚ a dátumom poslednej aktualizácie.
   - Pridaný medicínsky disclaimer (`#medical-disclaimer`) a archívny oznam ("Obsah tejto stránky je archívny (2023)..." s odkazom na `nefro.polascin.net`).
2. **Prepojenie v pätičke a cookie banneri:**
   - Aktualizovaný `blocks/footer.sk.php` a `blocks/cookie-banner.sk.php` s odkazmi na `/privacy.php` a `/privacy.php#medical-disclaimer`.
   - Pridaný redirect v `files/privacy.html` na `/privacy.php` pre spätnú kompatibilitu.
3. **SEO a sitemap:**
   - Upravený `sitemap.xml` a `ipvrm.htm` na kanonickú URL `/privacy.php`.
4. **CI/CD Automatický Deploy:**
   - Vytvorený a overený workflow `.github/workflows/deploy.yml` s podpora rsync/SSH cez kľúč `DEPLOY_SSH_KEY` a auto-scan host kľúča.
   - Overené úspešné nasadenie na produkčný server WebSupport (`shell.r1.websupport.sk`).
5. **Overenie živých HTTP odpovedí:**
   - `https://nephrosite.polascin.net/` → HTTP 200 OK
   - `https://nephrosite.polascin.net/privacy.php` → HTTP 301 / HTTP 200 OK
   - `https://nephrosite.polascin.net/files/privacy.html` → HTTP 200 OK

---

## Beh #2 – 2026-08-27

### Vykonané kontroly

- Bezpečnosť: živé HTTP hlavičky (`curl -sIL`), dostupnosť súkromných ciest (`/blocks/`, `/segments/`, `/ipvrm.files/`, `/.git/`, `*.md`, `phpstan.*`), kontrola `blocks/phpinfo.php`.
- HTML a sémantika: automatizovaná kontrola všetkých `<a target="_blank">` na `rel="noopener noreferrer"` a všetkých `<img>` na atribút `alt` (viacriadkovo, cez parser tagov).
- SEO: `<link rel="canonical">`, meta tagy, aktuálnosť `lastmod` v `sitemap.xml`, `robots.txt`.
- Prístupnosť: `:focus-visible`, `prefers-reduced-motion`, veľkosť touch targetov cookie lišty.
- GDPR: úplnosť `files/privacy.html` voči kontrolnému zoznamu §7.
- CI/CD: `deploy.yml` (php -l lint + rsync `--exclude-from=.deployignore`).
- Overenie: `php -l` (19 súborov), `.\tools\quality.ps1` (PHPStan 0 chýb, PHP-CS-Fixer 0/19), lokálny smoke test cez `php -S`.

### Nálezy a opravy

1. **KRITICKÝ – únik informácií cez `phpinfo()` (OWASP A05:2021):**
   - `blocks/phpinfo.php` bol verejne dostupný: `https://nephrosite.polascin.net/blocks/phpinfo.php` → **HTTP 200, 115 kB**, s výpisom PHP 8.5.9, absolútnych ciest, načítaných modulov a premenných prostredia.
   - Príčina: `.htaccess` mal pre `/blocks/` iba `X-Robots-Tag: noindex` (SEO), nie riadenie prístupu; `robots.txt` nie je bezpečnostný mechanizmus.
   - Oprava: súbor `blocks/phpinfo.php` **odstránený** + odstránený zakomentovaný odkaz naň z `blocks/footer.sk.php`.

2. **VYSOKÝ – priamy HTTP prístup k interným include fragmentom:**
   - `/blocks/*.php` a `/segments/*.html` vracali HTTP 200, hoci sa vkladajú iba cez PHP `include` z filesystemu.
   - Oprava: v `.htaccess` pridané `RewriteRule ^(?:blocks|segments)/ - [F,L]` (403). Overené, že v kóde neexistuje žiadne HTTP volanie ani odkaz na tieto cesty – iba `include`/`require`, ktoré blokovanie neovplyvňuje.

3. **STREDNÝ – reverse tabnabbing:**
   - **170** odkazov `target="_blank"` bez `rel="noopener noreferrer"` (`lectures.sk.php` 84, `promo.sk.php` 55, `footer.sk.php` 14, `intro.sk.php` 9, ďalšie 8).
   - Oprava: hromadne doplnené cez parser `<a>` tagov. Kontrola po oprave: 0 nechránených odkazov v zdrojoch aj vo vykreslenej stránke.

4. **STREDNÝ – neúplné GDPR informácie:**
   - `files/privacy.html` neuvádzal hosting ani spracúvanie serverových logov, hoci kontrolný zoznam §7 to vyžaduje (regresia oproti deklarácii v Behu #1).
   - Oprava: nová sekcia **„2. Hosting a serverové logy“** (WebSupport, s. r. o., SR/EÚ, žiadny prenos do tretích krajín, rozsah logov, právny základ čl. 6 ods. 1 písm. f) GDPR). Sekcie prečíslované na 1–15, kotva `#legal-disclaimer` zachovaná.

5. **NÍZKY – neaktuálny `sitemap.xml`:**
   - Všetky `lastmod` uvádzali `2026-03-22`, hoci súbory boli reálne zmenené neskôr.
   - Oprava: `lastmod` generovaný z reálneho času zmeny súborov (homepage z `index.php` + `blocks/`).

6. **NÍZKY – chýbajúci `<meta name="keywords">`** (kontrolný zoznam §3): doplnený do `blocks/metatags.sk.php` s odbornou nefrologickou terminológiou.

7. **NÍZKY – touch targety pod 44×44 px (WCAG 2.5.5):**
   - `.cookie-btn` bez `min-height` (~38 px), `.cookie-icon-button` 2,4 rem (38,4 px).
   - Oprava: `.cookie-btn { min-height: 44px; }`, `.cookie-icon-button` zväčšené na 2,75 rem (44 px).

8. **INFO – chýbajúci `charset` v HTTP hlavičke** pre statické `.html`: doplnené `AddDefaultCharset UTF-8` do `.htaccess`.

### Overené bez nálezu

- Všetky bezpečnostné hlavičky (`X-Frame-Options`, `X-Content-Type-Options`, HSTS, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-Opener-Policy`, `X-Permitted-Cross-Domain-Policies`) sú živo prítomné; `X-Powered-By` sa neodosiela.
- `/ipvrm.files/`, `*.md`, `phpstan.*` → 403. `<img>` bez `alt`: 0. `canonical` prítomný.
- `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')` použitý pri všetkých premenných výstupoch v `metatags.sk.php`.
- Git LFS súbory v `docs/` neboli modifikované (dodržané kritické obmedzenie).

### Poznatky pre ďalšie behy

- `noindex` (`X-Robots-Tag`, `robots.txt`) **nie je** riadenie prístupu – súkromné cesty vyžadujú `Require all denied` alebo `RewriteRule [F]`.
- Kontrola `rel="noopener"` musí byť viacriadková: atribúty formátované cez Prettier sú na samostatných riadkoch a jednoriadkový `grep` dáva falošné nálezy.
- Súbory v repozitári majú zmiešané konce riadkov (LF v gite, CRLF v pracovnej kópii cez `autocrlf`) – skripty na úpravu musia čítať a zapisovať s `newline=''`.

---

## Beh #3 – 2026-08-27

Kontrolné kolo po Behu #2 (podľa kroku 6: opakuj, kým sa nachádzajú nové bezpečne opraviteľné nedostatky).

### Vykonané kontroly

- Regresná kontrola opráv z Behu #2 na produkcii (phpinfo, blokovanie fragmentov, hlavičky, noopener).
- Štruktúra dokumentu verejných stránok v `files/` (`<!DOCTYPE>`, `lang`, `charset`, `viewport`, `<title>`).
- Štruktúra nadpisov vykreslenej homepage (počet `<h1>`).
- Hygiena nasadenia: ktoré súbory sa reálne dostávajú na produkčný server.

### Nálezy a opravy

1. **STREDNÝ – fragmenty v `sitemap.xml` ako indexovateľné stránky:**
   - `dialyza.html`, `nefrologia.html`, `purifikacia.html`, `transplantacia.html`, `usgo.html`, `nefamb.html`, `lipton-tx.html` boli v sitemape, ale sú to iba HTML fragmenty **bez** `<!DOCTYPE>`, `<html lang="sk">`, `<meta charset>`, `<meta name="viewport">` a `<title>`.
   - Zároveň sa vkladajú do `index.php` cez `require`, takže ten istý obsah je už na hlavnej stránke → **duplicitný obsah**. Nikde v kóde na ne nevedie žiadny odkaz.
   - Oprava: odstránené zo `sitemap.xml` (zostávajú 4 skutočné stránky) a v `.htaccess` označené `X-Robots-Tag: noindex` cez existujúci mechanizmus `noindex_path`. Súbory zostávajú dostupné, takže prípadné externé odkazy sa nerozbijú.

2. **STREDNÝ – 9× `<h1>` na homepage (WCAG 2.1, SEO):**
   - Každý vložený fragment prinášal vlastný `<h1>`; `blocks/intro.sk.php` a `blocks/regcitra.sk.php` mali ďalšie. Hlavná stránka pritom nemala žiadny nadpis popisujúci celý dokument.
   - Oprava: `<h1>` → `<h2>` vo všetkých 7 fragmentoch aj v `intro.sk.php` a `regcitra.sk.php`; do `blocks/ns.sk.php` pridaný jediný `<h1 class="sr-only">NephroSite – nefrológia, dialýza a interná medicína</h1>`.
   - Vizuál zachovaný: `.title-sc` a nová `.title-lg` majú `font-size: 2em; margin: 0.67em 0;` (zhodné s predvoleným `h1`). Výsledok: presne **1× `<h1>`**, 19× `<h2>`.

3. **NÍZKY – interné dokumenty sa nasadzovali na produkciu:**
   - `.audit.md`, `.doaudit.md`, `.agent.md` sa kopírovali na server (webovo blokované cez `.htaccess`, ale zbytočne prítomné).
   - Oprava: v `.deployignore` nahradené `DEPLOY.md` + `README.md` vzorom `*.md`.

### Overené bez nálezu

- Opravy z Behu #2 sú živo účinné: `/blocks/phpinfo.php` → **403** (bolo 200/115 kB), `/blocks/*.php` a `/segments/*.html` → **403**, `/` → 200, všetky verejné stránky → 200, `/terms.php` → 301.
- `Content-Type` teraz obsahuje `charset=UTF-8`; všetkých 7 bezpečnostných hlavičiek prítomných; `X-Powered-By` sa neodosiela.
- Vo vykreslenej homepage 0 odkazov `target="_blank"` bez `noopener`.
- `php -l` 19 súborov bez chýb, PHPStan 0 chýb, PHP-CS-Fixer 0/19, `sitemap.xml` well-formed.

### Poznatky pre ďalšie behy

- `files/*.html` majú dvojakú rolu: sú to `require` fragmenty **aj** URL adresy. Pri akejkoľvek zmene treba overiť oba režimy – pridanie `<head>`/`<title>` do fragmentu by na homepage vytvorilo vnorené `<html>`.
- Ak by mali byť tieto materiály samostatne indexovateľné, správnym riešením nie je úprava fragmentov, ale samostatné PHP obaly (wrapper) s vlastným `<head>`, ktoré fragment vložia – to je však obsahové rozhodnutie prevádzkovateľa, nie automatická oprava.
- Demotovanie nadpisov mení predvolenú veľkosť písma; vizuál treba dorovnať triedou v CSS, inak ide o neúmyselnú vizuálnu zmenu.

---

## Beh #4 – 2026-08-27 (uzatváracie kolo)

Regresná kontrola celého kontrolného zoznamu na produkcii po Behoch #2 a #3. **Žiadne nové nálezy** – cyklus podľa kroku 6 sa preto uzatvára.

| Kontrola | Výsledok |
| --- | --- |
| Bezpečnostné hlavičky (§1) | 7/7 prítomných, `X-Powered-By` sa neodosiela |
| `/blocks/phpinfo.php`, `/blocks/*`, `/segments/*` (§1) | 403 |
| `*.md`, `phpstan.*`, `/ipvrm.files/` (§1) | 403 |
| `target="_blank"` bez `noopener` (§2) | 0 |
| `<img>` bez `alt` (§2) | 0 |
| Počet `<h1>`, `lang="sk"`, skip-link (§2, §4) | 1× h1, `lang="sk"`, skip-link prítomný |
| `canonical`, `keywords`, `sitemap.xml` (§3) | prítomné; 4 skutočné stránky |
| Hosting + serverové logy, archívny oznam (§7) | prítomné |
| CI/CD deploy (§8) | GitHub Actions – success |

### Otvorené body pre prevádzkovateľa (nie sú to automatické opravy)

- **Samostatná indexovateľnosť medicínskych materiálov:** 7 fragmentov v `files/` je teraz `noindex` a mimo sitemapy, pretože ich obsah je duplicitne na homepage a samostatne nemajú `<head>`. Ak sa majú indexovať samostatne, treba vytvoriť PHP obaly s vlastným `<title>`, `canonical` a `<meta viewport>` – ide o obsahové rozhodnutie.
- **Lokálny `post-commit` hook** nasadzuje súbory podľa zmien v gite, takže `.deployignore` (ktorý platí pre GitHub Actions rsync) sa naň nevzťahuje; interné `*.md` sa preto stále môžu dostať na server, kde sú však webovo blokované (403).
- **Kontrast textu (§4)** a **overflow na 320–480 px (§5)** neboli merané automatizovane – vyžadujú vizuálnu kontrolu v prehliadači.

---

## Beh #5 – 2026-08-27

Realizácia otvoreného bodu z Behu #4: **samostatná indexovateľnosť medicínskych materiálov** (rozhodnutie prevádzkovateľa). Zvyšné dva otvorené body ostávajú podľa zadania nezmenené.

### Vykonané zmeny

1. **Nové tematické stránky (7):** `nefrologia.php`, `dialyza.php`, `purifikacia.php`, `transplantacia.php`, `usgo.php`, `nefamb.php`, `lipton-tx.php`.
   - Každá má vlastný `<title>` vo formáte „Téma | NephroSite“ (§3), `<meta name="description">`, vlastnú kanonickú URL, `lang="sk"`, `charset`, `viewport`, skip-link, hlavičku, pätu, cookie lištu aj JSON-LD s URL danej stránky.

2. **Zdieľaná šablóna `blocks/topic-page.php`:**
   - Fragment sa vykreslí cez `ob_start()` a jeho **prvý `<h2>` sa povýši na `<h1>`**, takže fragment ostáva jediným zdrojom obsahu – na homepage je `<h2>` pod hlavným nadpisom, na samostatnej stránke je `<h1>`. Žiadna duplicita textu.
   - `basename()` na ceste k fragmentu ako ochrana pred path traversal; neexistujúci fragment → HTTP 404.

3. **Parametrizácia existujúcich blokov (spätne kompatibilná cez `??=`):**
   - `blocks/metatags.sk.php`: `$pageTitle`, `$pageDescription`, `$pageKeywords`, `$ogImage`, nová `$canonicalUrl`; `og:url` a JSON-LD `WebPage` teraz používajú kanonickú URL a titulok danej stránky.
   - `blocks/links.sk.php`: `canonical` a `hreflang` z `$canonicalUrl`.
   - `blocks/ns.sk.php`: `$showSiteHeading` – skryté `<h1>` webu sa vykreslí len tam, kde stránka nemá vlastný hlavný nadpis. Homepage aj tematické stránky tak majú presne **1× `<h1>`**.

4. **Prepojenie (žiadne osirotené stránky):** do `blocks/info.sk.php` pridaných 6 odkazov „… – samostatná stránka“, do `blocks/foazosszu.sk.php` odkazy na `transplantacia.php` a `lipton-tx.php` (ten nebol na homepage vôbec).

5. **Staré fragmentové URL → 301:** `RewriteRule ^files/(dialyza|nefrologia|…)\.html$ /$1.php [R=301,L]`. Nahradilo predchádzajúci `noindex` z Behu #3 – externé odkazy teraz vedú na skutočný obsah namiesto stránky bez `<head>`. PHP `include` z filesystemu presmerovanie neovplyvňuje.

6. **`sitemap.xml`:** 7 nových stránok s prioritami 0,6–0,9 (spolu 11 URL).

7. **Upratanie:** `.skip-link` presunutý z inline `<style>` v `index.php` do `blocks/styles.css.php`, aby ho mali aj tematické stránky; pridané `.topic-breadcrumb`, `.topic-article`, `.topic-back`, `.topic-permalink`.

### Overenie

- `php -l` **27 súborov, 0 chýb**; PHPStan 0 chýb; PHP-CS-Fixer 0/27; `sitemap.xml` well-formed.
- Všetkých 7 stránok: 1× `<h1>`, 0 PHP chýb, správny titulok, vlastný `canonical`, platný JSON-LD.
- Homepage bez regresie: 1× `<h1>`, canonical `/`, pôvodný titulok, 9 odkazov na tematické stránky.

### Poznámka k duplicite obsahu

Homepage naďalej zobrazuje plné znenie šiestich tém a zároveň na ne odkazuje. Tematické stránky majú vlastný `canonical`, takže sú indexovateľné samostatne. Ak by mal byť prekryv úplne odstránený, ďalším krokom by bolo skrátiť sekcie na homepage na perex s odkazom „čítať ďalej“ – to je opäť obsahové rozhodnutie prevádzkovateľa.

---

## Beh #6 – 2026-08-27

Realizácia poslednej otvorenej poznámky z Behu #5: **odstránenie prekryvu obsahu medzi hlavnou stránkou a tematickými stránkami** (rozhodnutie prevádzkovateľa).

### Vykonané zmeny

1. **Nový blok `blocks/topic-teaser.php`:** z fragmentu v `files/` vyberie úvodný obrázok, prvý nadpis a prvý odstavec a doplní odkaz „Čítať ďalej: <téma> →“.
   - Obsah zostáva na jedinom mieste – vo fragmente. Hlavná stránka aj tematická stránka čítajú ten istý súbor, takže sa nemôžu rozísť.
   - Text odkazu obsahuje názov témy, takže je jednoznačný aj mimo kontextu (WCAG 2.4.4 – účel odkazu).

2. **`blocks/info.sk.php` prepísaný na perexy:** šesť sekcií (nefrológia, dialýza, purifikácia, transplantácia, USG obličiek, nefrologická ambulancia) zobrazuje namiesto celého znenia iba perex s odkazom. Zachované sú pôvodné farebné pozadia sekcií aj blok „Mimotelová eliminačná liečba“.

3. **`blocks/foazosszu.sk.php`:** druhé, duplicitné vloženie celého `files/transplantacia.html` nahradené rovnakým perexom; `segments/transplantacia.html` (iný obsah) ostáva bez zmeny. Odkaz na `lipton-tx.php` zachovaný.

4. **CSS:** pridané `.topic-teaser` a `.topic-more` (`clear: both` kvôli obtekaniu úvodného obrázka pri krátkom texte); využitá existujúca trieda `.clearfix`.

5. Redundantné odkazy `.topic-permalink` z Behu #5 pri sekciách odstránené – perex má vlastný odkaz „Čítať ďalej“.

### Výsledok

| Metrika | Pred | Po |
| --- | --- | --- |
| Text hlavnej stránky | 95 236 znakov | 58 782 znakov (**−38 %**) |
| Prekryv obsahu s tematickými stránkami | plné znenie 6 tém | iba perex (1 odstavec) |
| `<h1>` na hlavnej stránke | 1 | 1 |

### Overenie

- `php -l` 28 súborov, 0 chýb; PHPStan 0 chýb; PHP-CS-Fixer 0/28.
- Všetkých 7 perexov má presne 1 obrázok, 1 nadpis, 1 odstavec a 1 odkaz „Čítať ďalej“.
- Tematické stránky naďalej zobrazujú celé znenie (`dialyza.php`: 24 odstavcov, 1× `<h1>`); hlavná stránka 1× `<h1>`, 0 PHP chýb.

### Stav otvorených bodov z Behu #4

- Samostatná indexovateľnosť materiálov: **vyriešené** (Beh #5).
- Prekryv obsahu hlavnej stránky: **vyriešené** (Beh #6).
- Dosah lokálneho `post-commit` hooku mimo `.deployignore`: ponechané podľa rozhodnutia prevádzkovateľa.
- Vizuálna kontrola kontrastu a responzívnosti na 320–480 px: ponechané podľa rozhodnutia prevádzkovateľa.

---

## Beh #7 - 2026-08-28

Komplexná kontrola, oprava a optimalizácia všetkých interných a externých webových odkazov na stránkach (PHP bloky, HTML fragmenty a segmenty).

### Vykonané zmeny

1. **Oprava neplatnej domény v hlavičke (`blocks/ns.sk.php`):**
   - Nahradená neexistujúca doména `https://nephro.site/` (DNS NXDOMAIN) za platnú kanonickú adresu `https://nephrosite.polascin.net/`.

2. **Oprava odkazu na stiahnutie prednášky (`blocks/info.sk.php`):**
   - Odkaz na stiahnutie prednášky *Mimotelová eliminačná liečba* opravený z neexistujúceho lokálneho súboru `./docs/mel.pdf` na centrálne úložisko `https://sk.polascin.net/docs/mel.pdf`.
   - Opravený preklep v atribúte `title` pri vloženom iframe.

3. **Aktualizácia nefunkčných odkazov v prednáškach (`blocks/lectures.sk.php`):**
   - Staré 404 adresy na podstránky B. Braun nahradené platnými odkazmi na portál vzdelávacieho centra `https://czsk.aesculap-academy.com/`, `https://www.bbraun.sk/` a `https://www.bbraun.cz/`.

4. **Nahradenie nefunkčného Martinus JS widgetu (`files/lipton-tx.html`):**
   - Odstránený zastaraný a nefungujúci bannerový skript Martinus.sk (404) a nahradený priamym responzívnym partnerským odkazom na knihu *Biológia presvedčenia*.

5. **Odstránenie zrušeného analytického skriptu (`blocks/scripts.sk.php`):**
   - Odstránené volanie neaktívneho kontajnera Piwik Pro `1d43d282-1703-4cb7-9d66-91c82decad1a.sync.js` (404).

6. **Zabezpečenie HTTPS protokolov (`blocks/nephrology.sk.php`, `blocks/promo.sk.php`):**
   - Povýšené nešifrované adresy `http://sk.polascin.net/` a `http://www.europd.com/` na bezpečný protokol `https://`.

7. **Odstránenie neplatnej hlavičkovej značky (`ipvrm.htm`):**
   - Odstránená značka `<link rel="Edit-Time-Data" href="ipvrm.files/editdata.mso" />`.

8. **Validita HTML a typografia (`segments/IgAN.sk.html`, `blocks/archive-notice.sk.php`):**
   - V `segments/IgAN.sk.html` bol nadpis `<h4>` vyňatý z priameho vnorenia do zoznamu `<ol>`.
   - V `blocks/archive-notice.sk.php` bol text upravený bez použitia em dash.

9. **Prístupnosť a SEO na statických stránkach (`files/privacy.html`, `files/terms.html`):**
   - Doplnený `<link rel="canonical">` a sémantický obal `<main id="content">`.
   - Doplnený plne prístupný klávesnicový skip-link (`<a class="skip-link" href="#content">Preskočiť na obsah</a>`).

10. **Aktualizácia `sitemap.xml` a `robots.txt`:**
    - Všetky `lastmod` záznamy zaktualizované na `2026-08-28`.

### Overenie

- `php -l` **29 súborov, 0 syntaktických chýb**.
- `.\tools\quality.ps1` (PHPStan 0 chýb, PHP-CS-Fixer 0 chýb).
- Sémantická kontrola všetkých 10 stránok: 10/10 v poriadku (1x `<h1>`, `<!DOCTYPE>`, `lang="sk"`, UTF-8, viewport, canonical, skip-link, alt atribúty a `rel="noopener"`).
- Všetky interné aj externé odkazy otestované s nulovým počtom neplatných 404 volaní.
- Živý server vráti všetky bezpečnostné HTTP hlavičky vrátane HSTS a COOP.

---

## Beh #8 – 2026-09-04

Komplexný audit podľa kontrolného zoznamu po Behoch #1–#7 (bezpečnosť, HTML, SEO, WCAG, GDPR, jazyk, CI/CD).

### Vykonané kontroly

- Živé HTTP hlavičky a dostupnosť kľúčových URL (`/`, tematické `.php`, `privacy`/`terms`, PDF v `docs/`, blokované cesty).
- Regresia opráv z predchádzajúcich behov (`/blocks/`, `/segments/`, `*.md` → 403; 7/7 bezpečnostných hlavičiek).
- Sémantika vykreslených stránok: `lang="sk"`, 1× `<h1>`, skip-link, canonical, `rel="noopener noreferrer"`.
- GDPR konzistencia cookie lišty vs. skutočne načítavané analytické skripty.
- Jazyková kontrola UI textov (cookie banner, pätička, privacy/terms).
- `php -l`, `.\tools\quality.ps1`, lokálny render smoke test.

### Nálezy a opravy

1. **STREDNÝ – consent cookie bez `Secure` flagu:**
   - `blocks/scripts.sk.php` nastavoval `nephro_consent_v1` iba s `SameSite=Lax`.
   - Oprava: doplnené `Secure` pri zápise aj mazaní cookies (web je vynútene HTTPS).

2. **STREDNÝ – GDPR nesúlad (Piwik PRO):**
   - Po Behu #7 sa Piwik PRO už nenačítava, ale bol stále uvedený v `blocks/cookie-banner.sk.php` a `files/privacy.html`.
   - Oprava: odstránené z UI aj z právneho textu; zostávajú Clarity, Google Analytics a Fastbase.

3. **NÍZKY – HTTP odkaz:**
   - `blocks/lectures.sk.php`: `http://sk.polascin.net/` → `https://sk.polascin.net/`.

4. **NÍZKY – Schema.org:**
   - `medicalSpecialty: "Nephrologic"` → `"Nephrology"` v `blocks/metatags.sk.php`.

5. **NÍZKY – jazyková konzistencia (slovenčina):**
   - Cookie banner: „Privacy/Preference center“ a „disclaimer“ → slovenské ekvivalenty.
   - Pätička: anglické dátumy a FOSS poďakovanie preložené do slovenčiny; ClustrMaps na `https://` s slovenským `alt`/`title`.
   - `files/privacy.html` a `files/terms.html`: „disclaimer“ → „právne/zdravotnícke upozornenie“ (kotvy `#legal-disclaimer` / `#medical-disclaimer` zachované).

6. **NÍZKY – WCAG focus a overflow:**
   - Globálne `:focus-visible` pre odkazy a formulárové prvky; `overflow-x: hidden` na `body` (`blocks/styles.css.php`).

7. **NÍZKY – SEO hygieny:**
   - `sitemap.xml` `lastmod` → `2026-09-04`; `robots.txt` dátum aktualizácie.

8. **NÍZKY – XSS hygiena:**
   - Výstupy veku v `blocks/intro.sk.php` cez `htmlspecialchars(..., ENT_QUOTES, "UTF-8")`.

9. **INFO – lokálny nástroj kvality:**
   - `tools/quality.ps1`: PHPStan `--memory-limit=512M` (predvolených 128M padalo na tomto prostredí).

### Overené bez nálezu / ponechané

- Bezpečnostné hlavičky 7/7, `X-Powered-By` sa neodosiela; `/blocks/`, `/segments/`, `*.md` → 403.
- Všetkých 8 PHP stránok: 1× `<h1>`, `lang="sk"`, skip-link, canonical, 0 nechránených `target="_blank"`.
- `php -l` 0 chýb; PHPStan 0; PHP-CS-Fixer 0/28.
- **Ponechané:** `http://www.evuc.sk/` — HTTPS certifikát servera zlyháva; HTTP ostáva jedinou funkčnou voľbou.
- Git LFS súbory v `docs/` neboli modifikované.

### Poznatky pre ďalšie behy

- Po odstránení analytického skriptu treba vždy synchronizovať cookie UI a `privacy.html`.
- Consent cookies na HTTPS webe musia mať `Secure`; samotný `SameSite=Lax` nestačí.
- Externé odkazy overovať aj na dostupnosť TLS, nie len na existenciu HTTP odpovede.

---

## Beh #9 – 2026-09-04 (uzatváracie kolo)

Regresná kontrola po Behu #8. **Žiadne nové bezpečne opraviteľné nálezy** – cyklus podľa kroku 6 sa uzatvára.

| Kontrola | Výsledok |
| --- | --- |
| Bezpečnostné hlavičky (§1) | 7/7 prítomných |
| Blokované cesty (§1) | `/blocks/`, `/segments/`, `*.md` → 403 |
| Consent cookie `Secure` (§1, §7) | doplnené |
| `target="_blank"` bez `noopener` (§2) | 0 |
| `<h1>`, `lang="sk"`, skip-link (§2, §4) | OK na 8 stránkach |
| GDPR zoznam nástrojov (§7) | zladený so skriptami |
| `sitemap.xml` / `robots.txt` (§3) | aktualizované |
| Kvalita PHP (§8) | PHPStan 0, CS-Fixer 0 |

### Otvorené body pre prevádzkovateľa

- **`http://www.evuc.sk/`:** poskytovateľ nemá funkčný HTTPS; ponechané HTTP, kým nebude dostupná bezpečná alternatíva.
- **Vizuálna kontrola kontrastu a 320–480 px** v prehliadači (nie automatizovaná).
- **Lokálny `post-commit` hook** vs. `.deployignore` (známe z Behu #4).

### Commit, push a deploy

- Commit: `1021191` — *Harden consent cookies and align GDPR/Slovak UI after audit.*
- Push: `master` → `origin/master` (GitHub Actions run `33824806446` — **success**, ~19 s).
- Produkčný smoke: `https://nephrosite.polascin.net/` → HTTP 200, 7/7 bezpečnostných hlavičiek; naživo overené „Centrum ochrany súkromia“ a `SameSite=Lax; Secure`.
